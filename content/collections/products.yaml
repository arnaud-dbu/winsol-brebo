title: Products
template: products/show
revisions: false
route:
  nl: '/aanbod/{{ range_slug }}/{{ slug }}'
  fr: '/gamme/{{ range_slug }}/{{ slug }}'
  en: '/range/{{ range_slug }}/{{ slug }}'
sites:
  - nl
  - fr
  - en
