# `{{ img }}` Tag Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vervang `partial:img` door een custom `{{ img }}` PHP-tag met breakpoint-ratio's (server-side focal crops) en push de Glide-cache naar R2 zodat Cloudflare alle afbeeldingen serveert.

**Architecture:** Eén Statamic-tag (`app/Tags/Img.php`) die parameters normaliseert, per breakpoint Glide-URL's bouwt (`Image::manipulate()` met `fit: crop_focal`) en een Antlers-view (`resources/views/components/img.antlers.html`) rendert met de `<picture>`-markup. Glide-afgeleiden gaan naar een nieuwe `glide`-disk op de bestaande R2-bucket (root `img/`), aangezet via `GLIDE_CACHE` env zodat tests en dev zonder R2-credentials op de dynamische route blijven draaien.

**Tech Stack:** Statamic 6, Laravel 12, PHPUnit 11, Tailwind 4, Cloudflare R2 (S3-driver).

**Spec:** `docs/superpowers/specs/2026-06-10-img-tag-design.md`

---

## Vooraf: branch & werkboomstatus

De spec- en plan-commits staan op `feat/static-caching` (zoek ze op met `git log --oneline feat/static-caching -- docs/superpowers/specs/2026-06-10-img-tag-design.md docs/superpowers/plans/2026-06-10-img-tag.md`). Dit werk krijgt zijn eigen branch vanaf `main`. **Let op:** de werkboom bevat ongerelateerde, ongecommitte wijzigingen van het static-caching-werk (`config/statamic/static_caching.php`, `resources/fieldsets/steps.yaml`). Laat die staan; gebruik **nooit** `git add -A` of `git add .` — stage altijd expliciete paden.

- [ ] **Setup: branch aanmaken**

```bash
cd /Users/arnaud/Documents/github/statamic-base
git log --oneline feat/static-caching -- docs/superpowers/specs/2026-06-10-img-tag-design.md docs/superpowers/plans/2026-06-10-img-tag.md
git checkout main
git checkout -b feat/img-tag
git cherry-pick <spec-hash> <plan-hash>   # de twee docs-commits uit de log hierboven, oudste eerst
```

Verwacht: cherry-picks slagen zonder conflict (docs-only commits). De twee dirty bestanden blijven unstaged staan — niet aanraken.

---

### Task 1: Glide-cache disk op R2

**Files:**
- Modify: `config/filesystems.php` (na de `r2`-disk, regel ~43)
- Modify: `config/statamic/assets.php:55` (`'cache' => false`)
- Modify: `.env.example` (R2- en GLIDE-blok toevoegen)
- Test: `tests/Feature/GlideCacheConfigTest.php`

- [ ] **Step 1: Schrijf de failing test**

```php
<?php

namespace Tests\Feature;

use Tests\TestCase;

class GlideCacheConfigTest extends TestCase
{
    public function test_glide_disk_points_at_r2_img_root(): void
    {
        $disk = config('filesystems.disks.glide');

        $this->assertSame('s3', $disk['driver']);
        $this->assertSame(env('R2_BUCKET'), $disk['bucket']);
        $this->assertStringEndsWith('img', $disk['root']);
        $this->assertStringEndsWith('/img', $disk['url']);
    }

    public function test_glide_cache_is_env_driven_and_off_by_default(): void
    {
        // In tests is GLIDE_CACHE niet gezet: dynamische route blijft actief.
        $this->assertFalse(config('statamic.assets.image_manipulation.cache'));
    }
}
```

- [ ] **Step 2: Run de test, verwacht falen**

Run: `php artisan test --filter=GlideCacheConfigTest`
Verwacht: FAIL — `config('filesystems.disks.glide')` is `null`.

- [ ] **Step 3: Voeg de `glide`-disk toe aan `config/filesystems.php`**

Direct na het sluitende blok van de `r2`-disk (na regel 43):

```php
        'glide' => [
            'driver' => 's3',
            'key' => env('R2_ACCESS_KEY_ID'),
            'secret' => env('R2_SECRET_ACCESS_KEY'),
            'region' => 'auto',
            'bucket' => env('R2_BUCKET'),
            'url' => rtrim(env('R2_URL', ''), '/').'/img',
            'endpoint' => env('R2_ENDPOINT'),
            'root' => ltrim(trim(env('R2_ROOT', ''), '/').'/img', '/'),
            'use_path_style_endpoint' => false,
            'throw' => false,
        ],
```

- [ ] **Step 4: Maak de Glide-cache env-gestuurd in `config/statamic/assets.php`**

Vervang regel 55:

```php
        'cache' => false,
```

door:

```php
        'cache' => env('GLIDE_CACHE', false),
```

(`cache_path` op de regel eronder blijft staan; die geldt alleen voor `cache => true`.)

- [ ] **Step 5: Documenteer in `.env.example`**

Toevoegen onderaan `.env.example`:

```dotenv
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=
R2_URL=
R2_ENDPOINT=
R2_ROOT=

# Zet op "glide" om Glide-afgeleiden naar R2 te schrijven en direct
# vanaf R2/CDN te serveren. Eerste render per afbeelding is trager
# (synchronous generatie); daarna serveert Cloudflare alles.
GLIDE_CACHE=glide
```

- [ ] **Step 6: Run de test, verwacht groen**

Run: `php artisan test --filter=GlideCacheConfigTest`
Verwacht: PASS (2 tests).

- [ ] **Step 7: Commit**

```bash
git add config/filesystems.php config/statamic/assets.php .env.example tests/Feature/GlideCacheConfigTest.php
git commit -m "feat: add env-driven glide cache disk on R2"
```

> **Runtime-notitie (geen taakstap):** in productie/dev met R2-credentials zet je `GLIDE_CACHE=glide` in `.env`. Statamic gebruikt dan de `StaticUrlBuilder`: varianten worden bij de eerste page render gegenereerd, naar R2 geschreven en als directe R2-URL in de HTML gezet.

---

### Task 2: `{{ img }}` tag — basis `<picture>` zonder ratio

**Files:**
- Create: `app/Tags/Img.php`
- Create: `resources/views/components/img.antlers.html`
- Test: `tests/Feature/ImgTagTest.php`

- [ ] **Step 1: Schrijf de failing test**

```php
<?php

namespace Tests\Feature;

use Illuminate\Support\Facades\Storage;
use Statamic\Contracts\Assets\Asset;
use Statamic\Facades\Antlers;
use Statamic\Facades\AssetContainer;
use Tests\TestCase;

class ImgTagTest extends TestCase
{
    private function makeImageAsset(
        int $width = 1200,
        int $height = 800,
        array $data = [],
        string $filename = 'photo.jpg',
    ): Asset {
        Storage::fake('r2');

        $container = AssetContainer::make('assets')->disk('r2')->title('Assets');
        $container->save();

        if (str_ends_with($filename, '.svg')) {
            Storage::disk('r2')->put($filename, '<svg xmlns="http://www.w3.org/2000/svg"/>');
        } else {
            $image = imagecreatetruecolor($width, $height);
            ob_start();
            imagejpeg($image);
            Storage::disk('r2')->put($filename, ob_get_clean());
            imagedestroy($image);
        }

        $asset = $container->makeAsset($filename);
        $asset->data($data);
        $asset->save();

        return $asset;
    }

    private function render(string $template, array $context = []): string
    {
        return (string) Antlers::parse($template, $context);
    }

    public function test_renders_picture_with_webp_source_and_jpg_fallback(): void
    {
        $asset = $this->makeImageAsset(1200, 800);

        $html = $this->render('{{ img :src="image" alt="Een foto" }}', ['image' => $asset]);

        $this->assertStringContainsString('<picture>', $html);
        $this->assertStringContainsString('type="image/webp"', $html);
        $this->assertStringContainsString('fm=webp', $html);
        $this->assertStringContainsString('fm=jpg', $html);
        $this->assertStringContainsString('alt="Een foto"', $html);
        $this->assertStringContainsString('sizes="100vw"', $html);
        $this->assertStringContainsString('loading="lazy"', $html);
        $this->assertStringContainsString('decoding="async"', $html);
    }

    public function test_srcset_is_capped_at_intrinsic_width_and_max_width(): void
    {
        $asset = $this->makeImageAsset(1200, 800);

        $html = $this->render('{{ img :src="image" }}', ['image' => $asset]);

        // Ladder: 320/480/640/960/1280/1680/2560, cap = min(1680, 1200) = 1200.
        $this->assertStringContainsString('w=960', $html);
        $this->assertStringNotContainsString('w=1280', $html);
        $this->assertStringNotContainsString('w=2560', $html);

        // max_width verlaagt de cap verder.
        $html = $this->render('{{ img :src="image" max_width="640" }}', ['image' => $asset]);
        $this->assertStringContainsString('w=640', $html);
        $this->assertStringNotContainsString('w=960', $html);
    }

    public function test_without_ratio_dimensions_follow_intrinsic_aspect(): void
    {
        $asset = $this->makeImageAsset(1200, 800);

        $html = $this->render('{{ img :src="image" }}', ['image' => $asset]);

        // Fallback-breedte = min(1680, 1200) = 1200; hoogte schaalt mee: 800.
        $this->assertStringContainsString('width="1200"', $html);
        $this->assertStringContainsString('height="800"', $html);
        // Zonder ratio geen crop-parameters.
        $this->assertStringNotContainsString('fit=crop', $html);
    }
}
```

- [ ] **Step 2: Run de test, verwacht falen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: FAIL — tag `img` bestaat niet (output bevat de letterlijke tag of een tag-not-found-fout).

- [ ] **Step 3: Maak `app/Tags/Img.php`**

```php
<?php

namespace App\Tags;

use Statamic\Contracts\Assets\Asset;
use Statamic\Facades\Asset as AssetFacade;
use Statamic\Facades\Image;
use Statamic\Fields\Value;
use Statamic\Tags\Tags;

class Img extends Tags
{
    protected static $handle = 'img';

    private const WIDTHS = [320, 480, 640, 960, 1280, 1680, 2560];

    private const DEFAULT_MAX_WIDTH = 1680;

    private const DEFAULT_QUALITY = 85;

    public function index(): string
    {
        $asset = $this->resolveAsset($this->params->get('src'));

        if (! $asset) {
            return '';
        }

        $maxWidth = (int) $this->params->get('max_width') ?: self::DEFAULT_MAX_WIDTH;
        $quality = (int) $this->params->get('quality') ?: self::DEFAULT_QUALITY;
        $sizes = trim((string) $this->params->get('sizes')) ?: '100vw';
        $alt = trim((string) ($this->params->get('alt') ?? $asset->get('alt') ?? ''));

        $fallbackWidth = min($maxWidth, $asset->width());
        $widths = $this->srcsetWidths($asset->width(), $maxWidth);
        $fallbackHeight = (int) round($fallbackWidth * $asset->height() / $asset->width());

        $sources = [[
            'media' => null,
            'srcset' => $this->srcset($asset, $widths, null, 'webp', $quality),
            'width' => $fallbackWidth,
            'height' => $fallbackHeight,
        ]];

        return view('components.img', [
            'passthrough' => false,
            'sources' => $sources,
            'fallback_src' => $this->glideUrl($asset, $fallbackWidth, null, 'jpg', $quality),
            'fallback_srcset' => $this->srcset($asset, $widths, null, 'jpg', $quality),
            'width' => $fallbackWidth,
            'height' => $fallbackHeight,
            'sizes' => $sizes,
            'alt' => $alt,
            'class' => trim((string) $this->params->get('class')),
            'priority' => $this->params->bool('priority', false),
            'fill' => false,
            'focus_css' => '50% 50%',
            'data_speed' => $this->params->get('data_speed'),
        ])->render();
    }

    private function resolveAsset(mixed $src): ?Asset
    {
        if ($src instanceof Value) {
            $src = $src->value();
        }

        if ($src instanceof Asset) {
            return $src;
        }

        if (is_string($src) && $src !== '') {
            return AssetFacade::findByUrl($src);
        }

        return null;
    }

    private function srcsetWidths(int $assetWidth, int $maxWidth): array
    {
        $cap = min($assetWidth, $maxWidth);
        $widths = array_values(array_filter(self::WIDTHS, fn (int $w) => $w <= $cap));

        return $widths ?: [$cap];
    }

    private function srcset(Asset $asset, array $widths, ?float $ratio, string $format, int $quality): string
    {
        return collect($widths)
            ->map(fn (int $w) => $this->glideUrl($asset, $w, $ratio, $format, $quality)." {$w}w")
            ->implode(', ');
    }

    private function glideUrl(Asset $asset, int $width, ?float $ratio, string $format, int $quality): string
    {
        $manipulator = Image::manipulate($asset)
            ->width($width)
            ->format($format)
            ->quality($quality);

        if ($ratio) {
            $manipulator
                ->height((int) round($width / $ratio))
                ->fit('crop_focal');
        }

        return $manipulator->build();
    }
}
```

- [ ] **Step 4: Maak `resources/views/components/img.antlers.html`**

```antlers
{{ if passthrough }}
<img src="{{ src }}" alt="{{ alt | sanitize }}"{{ if class }} class="{{ class }}"{{ /if }}{{ if data_speed }} data-speed="{{ data_speed }}"{{ /if }}
    {{ if priority }}loading="eager" fetchpriority="high" decoding="async"{{ else }}loading="lazy" decoding="async"{{ /if }}>
{{ else }}
<picture>
    {{ sources }}
    <source type="image/webp"{{ if media }} media="{{ media }}"{{ /if }} width="{{ width }}" height="{{ height }}" sizes="{{ sizes }}" srcset="{{ srcset }}">
    {{ /sources }}
    <img src="{{ fallback_src }}" srcset="{{ fallback_srcset }}" sizes="{{ sizes }}" alt="{{ alt | sanitize }}" width="{{ width }}" height="{{ height }}"{{ if class }} class="{{ class }}"{{ /if }}{{ if fill }} style="object-position: {{ focus_css }}"{{ /if }}{{ if data_speed }} data-speed="{{ data_speed }}"{{ /if }}
        {{ if priority }}loading="eager" fetchpriority="high" decoding="async"{{ else }}loading="lazy" decoding="async"{{ /if }}>
</picture>
{{ /if }}
```

(De `passthrough`-branch wordt in Task 5 gebruikt; nu al aanwezig zodat de view niet meer hoeft te wijzigen.)

- [ ] **Step 5: Run de test, verwacht groen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: PASS (3 tests). Faalt `width="1200"`? Controleer dan of `$asset->width()` de fixture-afmetingen leest (meta-cache); debug met `dump($html)`.

- [ ] **Step 6: Commit**

```bash
git add app/Tags/Img.php resources/views/components/img.antlers.html tests/Feature/ImgTagTest.php
git commit -m "feat: add img tag with webp picture output"
```

---

### Task 3: ratio's per breakpoint met focal crops

**Files:**
- Modify: `app/Tags/Img.php`
- Test: `tests/Feature/ImgTagTest.php`

- [ ] **Step 1: Schrijf de failing tests (toevoegen aan `ImgTagTest`)**

```php
    public function test_ratio_crops_server_side_on_the_focal_point(): void
    {
        $asset = $this->makeImageAsset(1200, 800, ['focus' => '75-25-1']);

        $html = $this->render('{{ img :src="image" ratio="1/1" }}', ['image' => $asset]);

        // 1/1-crop: hoogte = breedte, focal point in de fit-parameter.
        $this->assertStringContainsString('w=480', $html);
        $this->assertStringContainsString('h=480', $html);
        $this->assertStringContainsString('fit=crop-75-25-1', $html);
        // width/height-attributen volgen de ratio (1200 cap, 1/1).
        $this->assertStringContainsString('width="1200"', $html);
        $this->assertStringContainsString('height="1200"', $html);
    }

    public function test_focal_crop_defaults_to_plain_crop_without_focus(): void
    {
        $asset = $this->makeImageAsset(1200, 800);

        $html = $this->render('{{ img :src="image" ratio="16/9" }}', ['image' => $asset]);

        // Geen focus-veld: crop_focal degradeert naar fit=crop (centrum).
        $this->assertStringContainsString('fit=crop', $html);
        $this->assertStringContainsString('h=270', $html); // 480 / (16/9)
    }

    public function test_breakpoint_ratios_render_media_sources_largest_first(): void
    {
        $asset = $this->makeImageAsset(2000, 1500, ['focus' => '50-50-1']);

        $html = $this->render(
            '{{ img :src="image" ratio="1/1" md:ratio="4/3" lg:ratio="16/9" }}',
            ['image' => $asset],
        );

        $this->assertStringContainsString('media="(min-width: 1024px)"', $html);
        $this->assertStringContainsString('media="(min-width: 768px)"', $html);

        // Grootste breakpoint eerst, basis-source (zonder media) als laatste.
        $lg = strpos($html, '(min-width: 1024px)');
        $md = strpos($html, '(min-width: 768px)');
        $base = strpos($html, '<source type="image/webp" width=');
        $this->assertLessThan($md, $lg);
        $this->assertLessThan($base, $md);

        // Per-source width/height: cap = min(1680, 2000) = 1680.
        $this->assertStringContainsString('height="945"', $html);  // 1680 / (16/9)
        $this->assertStringContainsString('height="1260"', $html); // 1680 / (4/3)
        $this->assertStringContainsString('height="1680"', $html); // 1680 / (1/1)
    }
```

- [ ] **Step 2: Run de tests, verwacht falen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: de 3 nieuwe tests FAIL (geen `h=`/`fit=crop`/`media=` in de output), de eerdere 3 blijven groen.

- [ ] **Step 3: Implementeer ratio-parsing en breakpoint-sources in `app/Tags/Img.php`**

Voeg bovenin de class een breakpoint-map toe:

```php
    private const BREAKPOINTS = [
        'sm' => 640,
        'md' => 768,
        'lg' => 1024,
        'xl' => 1280,
        '2xl' => 1536,
    ];
```

Voeg deze twee methods toe:

```php
    public static function parseRatio(?string $value): ?float
    {
        $value = trim((string) $value);

        if ($value === '') {
            return null;
        }

        if (! preg_match('#^(\d+(?:\.\d+)?)/(\d+(?:\.\d+)?)$#', $value, $m) || (float) $m[2] === 0.0) {
            throw_if(
                config('app.debug'),
                new \InvalidArgumentException("Ongeldige ratio \"{$value}\" voor {{ img }}; verwacht bv. \"16/9\".")
            );

            return null;
        }

        return (float) $m[1] / (float) $m[2];
    }

    /**
     * @return array{base: ?float, breakpoints: array<int, array{min: int, ratio: float}>}
     */
    private function ratios(): array
    {
        $breakpoints = collect(self::BREAKPOINTS)
            ->map(fn (int $min, string $prefix) => [
                'min' => $min,
                'ratio' => self::parseRatio($this->params->get("{$prefix}:ratio")),
            ])
            ->filter(fn (array $bp) => $bp['ratio'] !== null)
            ->sortByDesc('min')
            ->values()
            ->all();

        return [
            'base' => self::parseRatio($this->params->get('ratio')),
            'breakpoints' => $breakpoints,
        ];
    }
```

Vervang in `index()` het blok vanaf `$fallbackWidth = ...` t/m de `$sources = [...]`-toekenning door:

```php
        $ratios = $this->ratios();

        $fallbackWidth = min($maxWidth, $asset->width());
        $widths = $this->srcsetWidths($asset->width(), $maxWidth);
        $fallbackHeight = $ratios['base']
            ? (int) round($fallbackWidth / $ratios['base'])
            : (int) round($fallbackWidth * $asset->height() / $asset->width());

        $sources = collect($ratios['breakpoints'])
            ->map(fn (array $bp) => [
                'media' => "(min-width: {$bp['min']}px)",
                'srcset' => $this->srcset($asset, $widths, $bp['ratio'], 'webp', $quality),
                'width' => $fallbackWidth,
                'height' => (int) round($fallbackWidth / $bp['ratio']),
            ])
            ->push([
                'media' => null,
                'srcset' => $this->srcset($asset, $widths, $ratios['base'], 'webp', $quality),
                'width' => $fallbackWidth,
                'height' => $fallbackHeight,
            ])
            ->all();
```

En vervang in de `view(...)`-call de fallback-regels door:

```php
            'fallback_src' => $this->glideUrl($asset, $fallbackWidth, $ratios['base'], 'jpg', $quality),
            'fallback_srcset' => $this->srcset($asset, $widths, $ratios['base'], 'jpg', $quality),
```

- [ ] **Step 4: Run de tests, verwacht groen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add app/Tags/Img.php tests/Feature/ImgTagTest.php
git commit -m "feat: breakpoint ratios with server-side focal crops in img tag"
```

---

### Task 4: priority, fill, class-merge en alt-fallback

**Files:**
- Modify: `app/Tags/Img.php`
- Test: `tests/Feature/ImgTagTest.php`

- [ ] **Step 1: Schrijf de failing tests (toevoegen aan `ImgTagTest`)**

```php
    public function test_priority_renders_eager_with_fetchpriority(): void
    {
        $asset = $this->makeImageAsset();

        $html = $this->render('{{ img :src="image" priority="true" }}', ['image' => $asset]);

        $this->assertStringContainsString('loading="eager"', $html);
        $this->assertStringContainsString('fetchpriority="high"', $html);
        $this->assertStringNotContainsString('loading="lazy"', $html);
    }

    public function test_fill_adds_cover_classes_and_focal_object_position(): void
    {
        $asset = $this->makeImageAsset(1200, 800, ['focus' => '75-25-1']);

        $html = $this->render(
            '{{ img :src="image" fill="true" class="rounded-lg" }}',
            ['image' => $asset],
        );

        $this->assertStringContainsString(
            'class="absolute inset-0 w-full h-full object-cover rounded-lg"',
            $html,
        );
        $this->assertStringContainsString('style="object-position: 75% 25%"', $html);
    }

    public function test_class_lands_on_the_img_not_the_picture(): void
    {
        $asset = $this->makeImageAsset();

        $html = $this->render('{{ img :src="image" class="rounded-lg" }}', ['image' => $asset]);

        $this->assertStringContainsString('<picture>', $html);
        $this->assertStringNotContainsString('<picture class', $html);
        $this->assertStringContainsString('class="rounded-lg"', $html);
    }

    public function test_alt_falls_back_to_asset_alt_field(): void
    {
        $asset = $this->makeImageAsset(1200, 800, ['alt' => 'Alt uit asset']);

        $html = $this->render('{{ img :src="image" }}', ['image' => $asset]);
        $this->assertStringContainsString('alt="Alt uit asset"', $html);

        $html = $this->render('{{ img :src="image" alt="Override" }}', ['image' => $asset]);
        $this->assertStringContainsString('alt="Override"', $html);
    }
```

- [ ] **Step 2: Run de tests, verwacht falen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: `test_fill_adds_cover_classes...` FAIL (geen fill-classes/object-position); `test_alt_falls_back...` en `test_priority...` kunnen al groen zijn (logica bestond) — dat is oké, fill is de echte toevoeging.

- [ ] **Step 3: Implementeer fill en focal-css in `app/Tags/Img.php`**

Voeg deze method toe:

```php
    private function focusCss(Asset $asset): string
    {
        $focus = $asset->get('focus') ?? '50-50-1';
        [$x, $y] = array_pad(explode('-', $focus), 2, 50);

        return "{$x}% {$y}%";
    }
```

Vervang in `index()` de regel `'class' => trim(...)` en de regels `'fill' => false,` / `'focus_css' => '50% 50%',` door:

```php
        $fill = $this->params->bool('fill', false);
        $class = trim(
            ($fill ? 'absolute inset-0 w-full h-full object-cover ' : '')
            .trim((string) $this->params->get('class'))
        );
```

(boven de `view(...)`-call) en in de view-data:

```php
            'class' => $class,
            'fill' => $fill,
            'focus_css' => $this->focusCss($asset),
```

- [ ] **Step 4: Run de tests, verwacht groen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: PASS (10 tests).

- [ ] **Step 5: Commit**

```bash
git add app/Tags/Img.php tests/Feature/ImgTagTest.php
git commit -m "feat: priority, fill mode and alt fallback in img tag"
```

---

### Task 5: passthrough (svg/gif/extern) en foutafhandeling

**Files:**
- Modify: `app/Tags/Img.php`
- Test: `tests/Feature/ImgTagTest.php`

- [ ] **Step 1: Schrijf de failing tests (toevoegen aan `ImgTagTest`)**

```php
    public function test_svg_renders_as_plain_img_without_glide(): void
    {
        $asset = $this->makeImageAsset(filename: 'logo.svg', data: ['alt' => 'Logo']);

        $html = $this->render('{{ img :src="image" class="h-8" }}', ['image' => $asset]);

        $this->assertStringNotContainsString('<picture', $html);
        $this->assertStringNotContainsString('fm=webp', $html);
        $this->assertStringContainsString('class="h-8"', $html);
        $this->assertStringContainsString('alt="Logo"', $html);
        $this->assertStringContainsString('loading="lazy"', $html);
    }

    public function test_external_url_renders_passthrough_img(): void
    {
        $html = $this->render('{{ img src="https://example.com/foto.jpg" alt="Extern" }}');

        $this->assertStringContainsString('src="https://example.com/foto.jpg"', $html);
        $this->assertStringNotContainsString('<picture', $html);
        $this->assertStringNotContainsString('width=', $html);
    }

    public function test_missing_asset_renders_nothing(): void
    {
        config(['app.debug' => false]); // productiegedrag: stil falen

        $html = $this->render('{{ img src="/assets/bestaat-niet.jpg" }}');

        $this->assertSame('', trim($html));
    }

    public function test_missing_asset_throws_in_debug(): void
    {
        config(['app.debug' => true]);

        $this->expectException(\InvalidArgumentException::class);
        $this->render('{{ img src="/assets/bestaat-niet.jpg" }}');
    }

    public function test_invalid_ratio_throws_in_debug_and_is_ignored_in_production(): void
    {
        $asset = $this->makeImageAsset();

        config(['app.debug' => true]);
        $this->expectException(\InvalidArgumentException::class);
        $this->render('{{ img :src="image" ratio="16:9" }}', ['image' => $asset]);
    }

    public function test_invalid_ratio_is_ignored_without_debug(): void
    {
        $asset = $this->makeImageAsset(1200, 800);

        config(['app.debug' => false]);
        $html = $this->render('{{ img :src="image" ratio="16:9" }}', ['image' => $asset]);

        $this->assertStringNotContainsString('fit=crop', $html);
        $this->assertStringContainsString('height="800"', $html);
    }

    public function test_empty_string_params_fall_back_to_defaults(): void
    {
        $asset = $this->makeImageAsset(2000, 1500);

        // De oude partial-bug: max_width="" sizes="" mag niets breken.
        $html = $this->render(
            '{{ img :src="image" max_width="" sizes="" }}',
            ['image' => $asset],
        );

        $this->assertStringContainsString('sizes="100vw"', $html);
        $this->assertStringContainsString('w=1680', $html); // default max_width
    }
```

- [ ] **Step 2: Run de tests, verwacht falen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: svg- en extern-tests FAIL (renderen nu via `<picture>` of crashen op `width()`); empty-params en invalid-ratio kunnen al groen zijn door eerdere normalisatie.

- [ ] **Step 3: Implementeer passthrough in `app/Tags/Img.php`**

Vervang het begin van `index()` (t/m de `if (! $asset)`-check) door:

```php
    public function index(): string
    {
        $src = $this->params->get('src');
        $asset = $this->resolveAsset($src);

        if (! $asset) {
            if (is_string($src) && str_starts_with($src, 'http')) {
                return $this->passthrough($src);
            }

            throw_if(
                config('app.debug') && ! blank($src),
                new \InvalidArgumentException('{{ img }}: asset niet gevonden voor src "'.(is_string($src) ? $src : gettype($src)).'".')
            );

            return '';
        }

        if (in_array(strtolower($asset->extension()), ['svg', 'gif'], true)) {
            return $this->passthrough($asset->url(), $asset->get('alt') ?? '');
        }
```

Voeg de `passthrough`-method toe:

```php
    private function passthrough(string $url, string $assetAlt = ''): string
    {
        return view('components.img', [
            'passthrough' => true,
            'src' => $url,
            'alt' => trim((string) ($this->params->get('alt') ?? $assetAlt)),
            'class' => trim((string) $this->params->get('class')),
            'priority' => $this->params->bool('priority', false),
            'data_speed' => $this->params->get('data_speed'),
        ])->render();
    }
```

Let op: Antlers kan tag-exceptions wrappen. Faalt een `expectException`-test omdat een andere exception-class naar buiten komt, assert dan op de wrapper en controleer `getPrevious()` — pas niet de tag-code aan om de test te plezieren.

- [ ] **Step 4: Run de tests, verwacht groen**

Run: `php artisan test --filter=ImgTagTest`
Verwacht: PASS (17 tests).

- [ ] **Step 5: Commit**

```bash
git add app/Tags/Img.php tests/Feature/ImgTagTest.php
git commit -m "feat: svg/gif/external passthrough and error handling in img tag"
```

---

### Task 6: call sites migreren en partial verwijderen

**Files:**
- Modify: `resources/views/articles/show.antlers.html:9`
- Modify: `resources/views/partials/article.antlers.html:3`
- Modify: `resources/views/partials/case.antlers.html:3` (en stray regel 17 verwijderen)
- Modify: `resources/views/partials/sections/text.antlers.html:6`
- Modify: `resources/views/partials/sections/imageGallery.antlers.html:2,10,16,22,28`
- Delete: `resources/views/partials/img.antlers.html`

- [ ] **Step 1: `articles/show.antlers.html`**

Oud (regel 9):
```antlers
{{ partial:img :image="{{image.url}}" alt="{{image.alt}}" max_width="" sizes="" }}
```
Nieuw:
```antlers
{{ img :src="image" :alt="image.alt" max_width="960" sizes="(min-width: 768px) 768px, 100vw" }}
```
(Artikelbody in `container-sm`; 960 dekt retina af.)

- [ ] **Step 2: `partials/article.antlers.html`**

Oud (regel 3):
```antlers
{{ partial:img class="aspect-video" :image="image.url" :alt="image.alt" max_width="" sizes="" }}
```
Nieuw:
```antlers
{{ img :src="image" :alt="image.alt" ratio="16/9" max_width="960" sizes="33vw" }}
```
(Kaart in `grid-cols-3` op `articles/index`; de CSS-crop `aspect-video` wordt een servercrop.)

- [ ] **Step 3: `partials/case.antlers.html`**

Zelfde vervanging als Step 2 op regel 3. Verwijder daarnaast de stray regel 17 (`{{ img :image="image.url" :alt="image.alt" max_width="" sizes="" }}` — restant van een eerder experiment; gebruikt bovendien `:image` i.p.v. `:src` en zou nu een debug-exception geven).

- [ ] **Step 4: `partials/sections/text.antlers.html`**

Oud (regel 6):
```antlers
{{ partial:img :image="image.url" class="max-w-sm" sizes="" max_width="" }}
```
Nieuw:
```antlers
{{ img :src="image" class="max-w-sm" max_width="960" sizes="(min-width: 448px) 384px, 100vw" }}
```
(`max-w-sm` = 384px; 960 dekt retina af; alt valt terug op het asset-veld.)

- [ ] **Step 5: `partials/sections/imageGallery.antlers.html`**

Verwijder regel 2 (`{{ gridImage = "aspect-square" }}`) en vervang de vier call sites; `class="{{ gridImage }}"` vervalt (de vierkante crop is nu server-side):

```antlers
{{# 1/1 #}}
{{ img :src="url" ratio="1/1" max_width="1680" sizes="100vw" }}
{{# 1/2 #}}
{{ img :src="url" ratio="1/1" max_width="960" sizes="(min-width: 640px) 50vw, 100vw" }}
{{# 1/3 #}}
{{ img :src="url" ratio="1/1" max_width="640" sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw" }}
{{# 1/4 #}}
{{ img :src="url" ratio="1/1" max_width="640" sizes="(min-width: 1280px) 25vw, (min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw" }}
```

- [ ] **Step 6: Verwijder de oude partial en verifieer**

```bash
rm resources/views/partials/img.antlers.html
grep -rn "partial:img" resources/views && echo "NOG REFERENTIES" || echo "schoon"
php artisan test
npm run build
```

Verwacht: `schoon`; testsuite groen **behalve** de bekende pre-existing failure (`AssetUploadCompressionTest`/image-compression, 128M-geheugenlimiet — geen regressie van dit werk); Vite-build slaagt (de `aspect-video`/`aspect-square`-classes verdwijnen uit de CSS, dat is verwacht).

- [ ] **Step 7: Handmatige render-check**

```bash
php artisan serve
```

Open een pagina met afbeeldingen (bv. articles-index) en controleer in de browser-devtools: `<picture>` met webp-source, kloppende `width`/`height`, geen 404's op Glide-URL's, focuspunt zichtbaar gerespecteerd bij `ratio`-crops.

- [ ] **Step 8: Commit**

```bash
git add resources/views/articles/show.antlers.html resources/views/partials/article.antlers.html resources/views/partials/case.antlers.html resources/views/partials/sections/text.antlers.html resources/views/partials/sections/imageGallery.antlers.html
git rm resources/views/partials/img.antlers.html
git commit -m "feat: migrate views to img tag and remove img partial"
```

---

## Na afloop

- PR openen van `feat/img-tag` naar `main` (niet stapelen op `feat/static-caching`; de spec-commit zit via cherry-pick in beide — git dedupliceert bij merge).
- In productie/staging: `GLIDE_CACHE=glide` zetten in `.env` en éénmalig `php please static:clear` (static cache bevat nog oude `/img/...`-URL's).
- Buiten scope (bewust, zie spec): AVIF, LQIP, Cloudflare Image Transformations, preset-prewarming.
